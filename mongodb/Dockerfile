# This Dockerfile is optional for running MongoDB via docker-compose, but here is a simple example if you want to build a custom MongoDB image.

FROM mongo:7
# You can add custom configuration or initialization scripts here if needed.
# COPY ./init-mongo.js /docker-entrypoint-initdb.d/

# Expose the default MongoDB port
EXPOSE 27017